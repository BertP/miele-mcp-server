version: '3.8'

services:
  miele-mcp-server:
    build: .
    container_name: miele-mcp-server
    restart: unless-stopped
    ports:
      - "8089:8089"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
